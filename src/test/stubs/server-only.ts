// Test stub for Next.js' `server-only` package. The real package throws at
// import time if a module is included in a client bundle. For vitest we just
// need an empty module so `import 'server-only'` resolves.
export {};
