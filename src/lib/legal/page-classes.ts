/** Tailwind class strings specific to the legal pages
 *  (/terms, /seller-terms, /privacy, /cookies).
 *
 *  The H2 section heading lives in `@/lib/heading-classes` since it's
 *  project-wide. This file keeps only the legal-specific subheading
 *  variant (note `pt-1` for tighter vertical rhythm inside long-form
 *  prose). */

export const LEGAL_SUB_HEADING_CLASS =
  'text-base font-semibold text-semantic-text-heading pt-1';
